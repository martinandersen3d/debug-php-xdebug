{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug",
            "type": "php",
            "request": "launch",
            "port": 9000
            //"ignore": ["**/vendor/**/*.php"] //An optional array of glob patterns that errors should be ignored from (for example **/vendor/**/*.php

        },
        {

        "name": "Launch currently open script",
        "type": "php",
        "request": "launch",
        "program": "${file}",
        "cwd": "${fileDirname}",
        "port": 9000
        }
    ]
}